/**
 * Stanford Benchmark: Fibonacci Sequence
 * Classic benchmark for compiler optimization testing
 */

int fib(int n) {
    if (n <= 1) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

int main() {
    int result = fib(10);
    return result;
}