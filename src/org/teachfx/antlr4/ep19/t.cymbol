struct Point {
    int x;
    int y;
}

int fib(int n) {
    if (n <= 2) return n;
    else return fib(n-1) + fib(n-2);
}

int main() {
    Point s = new Point();
    int y = 12;
    s.x = 10 + fib(6);
    s.y = 2  + y;
    print(s.x);
    return 0;
}