# Chapter Prompt Examples

本文件包含已经填好的章节提示词示例，可以直接发送给技术写作代理生成章节内容。

---

## 示例 1：第1章（基础工作台）

### 章节写作任务

请为本书撰写一整章内容。

### 本章基本信息

- **章节标题**: 第1章：为人和 AI 搭建最小工作台
- **所在模块**: 模块 1：基础语言与解释器（EP1–EP12）
- **面向读者**: 会 Java、有 Maven 使用经验，未必系统学过编译原理，但对"自己做一门小语言"有兴趣的工程师
- **读者前置知识**: Java 基础语法、命令行、基本 Git 操作
- **本章对应仓库 EP 范围**: EP1–EP2（环境搭建 + 最小语法）
- **本章在全书中的位置**:
  - 前一章：第0章：为什么编译器是训练 AI 协作的最佳场景
  - 后一章：第2章：表达式、运算与解释器基础
- **本章完成后，读者应该能**:
  - 在本地拉起 `How_to_implment_PL_in_Antlr4` 项目并成功构建
  - 理解项目的多模块结构和 EP 概念
  - 让 ANTLR4 为最小 Cymbol 语法生成 lexer/parser，并跑通一个最简单的示例

---

## 内容结构要求（必须按顺序给出）

### 1. 本章概述

用 1–3 句话说明本章要解决什么问题、处在整个编译器流水线哪一环，明确本章的学习价值。

### 2. 动机与真实场景

用工程实战的小故事/场景，引出本章主题。说明如果没有这一章的能力，在真实项目中会遇到什么痛点。

### 3. 人类工程师线：技术与实现

#### 3.1 核心概念

用通俗语言解释本章关键概念（ANTLR4、语法文件、词法分析、语法分析）。必须包含 1–3 个小图示的文字描述（用 `[图X：描述]` 占位）。使用比喻和类比降低理解难度。

#### 3.2 与仓库 EP 的对应关系

明确说明：
- 对应目录：`antlr4-project/ep1/` 和 `ep2/`
- 关键文件：`Hello.g4`、`ArrayInit.g4`、`Test.java`
- ANTLR4 Maven 插件配置（`pom.xml`）

不需要贴完整代码，只贴关键语法规则、关键代码片段，使用 ```java 代码块，并加上详尽中文注释。

#### 3.3 实战流程

给出从命令行/IDE 运行本章代码的具体步骤：
- 进入哪个目录
- 运行哪些 Maven 命令或测试类
- 预期输出长什么样

### 4. AI 协作线：Context Engineering 视角

#### 4.1 上下文设计

解释：为了让 AI 帮忙完成本章任务，你会给 AI 提供哪些「上下文」？按类型列出源码文件、README/设计文档、示例输入/输出、测试类。对上下文做简短的组织说明。

#### 4.2 Prompt 模板（给 AI 用）

给出 1–2 段可以直接复制给 AI 的 Prompt 模板。例如：
- 「请帮我搭建 ANTLR4 开发环境，包括 Maven 依赖配置和 IDEA 插件安装...」
- 「请解释这个 Hello.g4 语法文件的每个部分，说明词法规则和语法规则的区别...」

#### 4.3 AI 应该做 / 不该做

列出 3–5 条本章相关的「允许 AI 做的事情」和「禁止 AI 做的事情」，特别是：
- 允许：配置开发环境、生成 ANTLR4 代码、解释语法规则
- 禁止：修改全局 Maven 配置、删除现有文件、创建新的 EP 模块

#### 4.4 验证与回滚策略

告诉读者：在接受 AI 的修改前，至少要做哪些验证？运行哪些测试；手工检查哪些关键点。如果 AI 修改后出现问题，有什么简单的回滚方案（例如 git 操作建议）。

### 5. 练习题

请设计 3–5 道练习，分为两类：
- 「手工实现版」：读者完全自己动手，不依赖 AI
- 「AI 协作版」：读者设计上下文和 Prompt，让 AI 辅助完成

每道题后附一个简短的「解题思路提示」，但不要给出完整参考答案。

### 6. 本章小结与下一章预告

用短短数段话总结本章关键收获；明确指出这些收获将如何在下一章中被用到（例如：最小语法将被扩展为表达式求值）。

**硬性要求**：
- 如果你在本章中没有给出至少 **1 个可直接复制给 AI 的 Prompt 模板**，请自动补齐。
- 如果你在本章中没有说明**如何验证 AI 的输出**，请自动补齐一个独立小节。

---

## 示例 2：第12章（SSA 与数据流基础）

### 章节写作任务

请为本书撰写一整章内容。

### 本章基本信息

- **章节标题**: 第12章：SSA 与数据流基础
- **所在模块**: 模块 3：现代编译器与优化（EP17–EP21）
- **面向读者**: 已经实现过解释器与简单编译器，熟悉 IR 概念的工程师
- **读者前置知识**: 基本控制流图（CFG）概念、三地址 IR、Java 中集合与图结构的使用
- **本章对应仓库 EP 范围**: EP21（SSA 与数据流分析）
- **本章在全书中的位置**:
  - 前一章：第11章：虚拟机设计与垃圾回收
  - 后一章：第13章：控制流图与优化策略
- **本章完成后，读者应该能**:
  - 解释什么是 SSA 形式、φ 函数的作用
  - 理解仓库中 SSA 转换的主要数据结构与算法轮廓
  - 设计基础的 SSA 测试用例，并用工具生成与检查 SSA 结果
  - 利用 AI 帮助完成部分 SSA 转换、测试生成工作，但知道哪些地方必须人工审查

---

## 内容结构要求（必须按顺序给出）

### 1. 本章概述

用 1–3 句话说明本章要解决什么问题、处在整个编译器流水线哪一环。

### 2. 动机与真实场景

用贴近工程实战的小故事/场景，引出本章主题。让读者知道：如果没有这一章的能力，在真实项目中会遇到什么痛点（例如：数据流分析对于优化至关重要，但传统的三地址码难以直接优化）。

### 3. 人类工程师线：技术与实现

#### 3.1 核心概念

用通俗语言解释本章关键概念（SSA 形式、φ 函数、数据流分析）。必须包含 1–3 个小图示的文字描述（用 `[图X：描述]` 占位）。

在"核心概念"部分，用一个"变量版本号（x0/x1/x2）"的类比故事解释 SSA：
- 想象你在处理一份账单，每次修改都创建新版本（x0 → x1 → x2）
- φ 函数就像会计师的汇总表，告诉你不同来源应该用哪个版本
- 这样每次看到 x1，你确切知道它是从哪条路径来的

#### 3.2 与仓库 EP 的对应关系

明确说明：
- 对应目录：`antlr4-project/ep21/src/main/java/org/teachfx/antlr4/ep21/pass/ssa/`
- 关键类：`SSAConverter`、`PhiFunction`、`DominanceTree`
- 关键方法：`convertToSSA()`、`placePhiFunctions()`、`renameVariables()`

不需要贴完整代码，只贴关键方法，使用 ```java 代码块展示伪代码或精简版代码。

#### 3.3 实战流程

给出从命令行/IDE 运行本章代码的具体步骤：
- 进入哪个目录
- 运行哪些 Maven 命令或测试类
- 预期输出长什么样

若本章涉及可视化（CFG/SSA 等），说明如何生成 `.dot`/`.png`。

### 4. AI 协作线：Context Engineering 视角

#### 4.1 上下文设计

解释：为了让 AI 帮忙完成本章任务，你会给 AI 提供哪些「上下文」？按类型列出源码文件、README/设计文档、示例输入/输出、测试类。对上下文做简短的组织说明。

#### 4.2 Prompt 模板（给 AI 用）

给出 1–2 段可以直接复制给 AI 的 Prompt 模板，例如：
- 「请根据以下 IR 规则，将这段三地址码转换为 SSA 形式...」
- 「请为我设计一个 SSA 转换的测试用例，验证 φ 函数的正确放置...」

#### 4.3 AI 应该做 / 不该做

列出 3–5 条本章相关的「允许 AI 做的事情」和「禁止 AI 做的事情」，特别是：
- 允许：实现 SSA 转换算法、生成测试用例、生成图表描述
- 禁止：修改控制流结构、改变变量定义顺序、删除现有测试

#### 4.4 验证与回滚策略

告诉读者：在接受 AI 的修改前，至少要做哪些验证？运行哪些测试；手工检查哪些关键点。如果 AI 修改后出现问题，有什么简单的回滚方案（例如 git 操作建议）。

### 5. 练习题

请设计 3–5 道练习，分为两类：
- 「手工实现版」：读者完全自己动手，不依赖 AI
- 「AI 协作版」：读者设计上下文和 Prompt，让 AI 辅助完成

每道题后附一个简短的「解题思路提示」，但不要给出完整参考答案。

### 6. 本章小结与下一章预告

用短短数段话总结本章关键收获；明确指出这些收获将如何在下一章中被用到（例如：SSA 将被用于数据流优化）。

**硬性要求**：
- 如果你在本章中没有给出至少 **1 个可直接复制给 AI 的 Prompt 模板**，请自动补齐。
- 如果你在本章中没有说明**如何验证 AI 的输出**，请自动补齐一个独立小节。

---

## 使用说明

1. 复制上述任一示例章节提示词
2. 发送给技术写作代理（配合 SYSTEM_PROMPT.md 使用）
3. 技术写作代理将严格按照提示词结构和要求生成章节内容
4. 可以根据需要修改占位符内容，生成更多章节提示词

---

**文件版本**: 1.0
**最后更新**: 2026-01-12
**状态**: ✅ 就绪
