# EP18R 详细任务分解

**版本**: v1.0 | **日期**: 2026-01-08 | **状态**: 任务分解完成
**目的**: 提供EP18R项目的详细任务分解和实施指南
**相关文档**: [架构设计规范](架构设计规范.md) | [改进计划](改进计划.md) | [TDD执行计划_精简版](TDD执行计划_精简版.md)

---

## 1. 任务分解概述

### 1.1 任务分类

EP18R项目任务分为以下类别：

| 类别 | 任务数 | 总工时 | 状态 |
|------|--------|--------|------|
| **基础设施恢复** | 2 | 10小时 | ⏸️ 未开始 |
| **代码重复消除** | 3 | 28小时 | ⏸️ 未开始 |
| **上帝类拆分** | 3 | 36小时 | ⏸️ 未开始 |
| **接口抽象优化** | 3 | 30小时 | ⏸️ 未开始 |
| **测试增强** | 2 | 28小时 | ⏸️ 未开始 |
| **文档完善** | 2 | 10小时 | ⏸️ 未开始 |

### 1.2 任务状态说明

- **✅ 已完成**: 任务已完成并验收
- **🔄 进行中**: 任务正在执行
- **⏸️ 未开始**: 任务尚未开始
- **🔴 高优先级**: 必须优先完成的任务
- **🟡 中优先级**: 重要的但可延后的任务
- **🟢 低优先级**: 可选的增强任务

---

## 2. 基础设施恢复任务

### TASK-BINFRA-001: 恢复代码质量工具配置

**优先级**: 🔴 高 | **预计工时**: 4小时 | **依赖**: 无

#### 2.1.1 任务描述

在 `ep18r/pom.xml` 中添加完整的代码质量工具配置，包括 CheckStyle、SpotBugs 和 JaCoCo。

#### 2.1.2 详细步骤

**步骤 1: 添加 CheckStyle 插件** (1小时)
```xml
<plugin>
  <groupId>org.apache.maven.plugins</groupId>
  <artifactId>maven-checkstyle-plugin</artifactId>
  <version>3.2.1</version>
  <configuration>
    <configLocation>google_checks.xml</configLocation>
  </configuration>
</plugin>
```

**步骤 2: 添加 SpotBugs 插件** (1小时)
```xml
<plugin>
  <groupId>com.github.spotbugs</groupId>
  <artifactId>spotbugs-maven-plugin</artifactId>
  <version>4.7.3.0</version>
</plugin>
```

**步骤 3: 添加 JaCoCo 插件** (1.5小时)
```xml
<plugin>
  <groupId>org.jacoco</groupId>
  <artifactId>jacoco-maven-plugin</artifactId>
  <version>0.8.10</version>
  <executions>
    <execution>
      <goals><goal>prepare-agent</goal></goals>
    </execution>
    <execution>
      <id>report</id>
      <phase>test</phase>
      <goals><goal>report</goal></goals>
    </execution>
  </executions>
</plugin>
```

**步骤 4: 验证配置** (0.5小时)
```bash
mvn checkstyle:check
mvn spotbugs:check
mvn test jacoco:report
```

#### 2.1.3 验收标准

- [ ] `mvn checkstyle:check` 无错误
- [ ] `mvn spotbugs:check` 无严重问题
- [ ] `mvn test jacoco:report` 生成覆盖率报告
- [ ] 所有现有测试通过

#### 2.1.4 成功标准

- 代码质量工具配置完整
- CI/CD 集成成功
- 无配置错误或冲突

---

### TASK-BINFRA-002: 建立基线测试覆盖

**优先级**: 🔴 高 | **预计工时**: 6小时 | **依赖**: TASK-BINFRA-001

#### 2.2.1 任务描述

运行现有测试，生成初始覆盖率报告，识别低覆盖率区域，创建覆盖率差距分析文档。

#### 2.2.2 详细步骤

**步骤 1: 运行完整测试套件** (1小时)
```bash
mvn clean test
```

**步骤 2: 生成覆盖率报告** (1.5小时)
```bash
mvn jacoco:report
open target/site/jacoco/index.html
```

**步骤 3: 分析低覆盖率区域** (2小时)
- 识别覆盖率 < 70% 的类
- 重点分析以下区域：
  - GC 系统（ReferenceCountingGC）
  - 异常处理（Exception classes）
  - 边缘情况（Edge cases）
  - 工具类（CallingConventionUtils）

**步骤 4: 创建覆盖率差距分析文档** (1.5小时)
- 创建 `docs/CoverageGapAnalysis.md`
- 记录每个低覆盖率类的分析
- 提出改进建议

#### 2.2.3 验收标准

- [ ] 生成完整的覆盖率报告
- [ ] 识别出所有覆盖率 < 70% 的类
- [ ] 创建覆盖率差距分析文档
- [ ] 提出具体的改进建议

#### 2.2.4 成功标准

- 建立测试覆盖率基线
- 明确改进方向和优先级
- 文档清晰可读

---

## 3. 代码重复消除任务

### TASK-DUPL-001: 提取通用寄存器操作工具类

**优先级**: 🔴 高 | **预计工时**: 8小时 | **依赖**: 无

#### 3.1.1 任务描述

创建 `RegisterOperandExtractor` 类，消除重复的寄存器操作逻辑。

#### 3.1.2 详细步骤

**步骤 1: 创建 RegisterOperandExtractor 类** (2小时)
```java
// 新类：RegisterOperandExtractor.java
public class RegisterOperandExtractor {
    public static int extractRd(int operand) {
        return (operand >> 21) & 0x1F;
    }
    
    public static int extractRs1(int operand) {
        return (operand >> 16) & 0x1F;
    }
    
    public static int extractRs2(int operand) {
        return (operand >> 11) & 0x1F;
    }
    
    public static int extractImm16(int operand) {
        int imm = operand & 0xFFFF;
        if ((imm & 0x8000) != 0) {
            imm |= 0xFFFF0000;
        }
        return imm;
    }
    
    public static int extractImm26(int operand) {
        int imm = operand & 0x3FFFFFF;
        if ((imm & 0x2000000) != 0) {
            imm |= 0xFC000000;
        }
        return imm;
    }
}
```

**步骤 2: 替换 RegisterVMInterpreter 中的重复代码** (2小时)
- 删除重复的 extract* 方法
- 导入 RegisterOperandExtractor
- 替换所有调用

**步骤 3: 替换 ControlFlowExecutors 中的重复代码** (2小时)
- 删除重复的 extract* 方法
- 导入 RegisterOperandExtractor
- 替换所有调用

**步骤 4: 替换其他执行器类中的重复代码** (2小时)
- MemoryExecutors
- ArithmeticExecutors
- ComparisonExecutors
- 等等...

#### 3.1.3 验收标准

- [ ] `RegisterOperandExtractor` 类创建完成
- [ ] 所有重复的 `extract*` 方法替换为工具类调用
- [ ] 所有测试通过
- [ ] 代码重复率降低 ≥ 20%

#### 3.1.4 成功标准

- 寄存器操作逻辑统一
- 代码重复显著减少
- 易于维护和扩展

---

### TASK-DUPL-002: 创建指令执行模板

**优先级**: 🔴 高 | **预计工时**: 12小时 | **依赖**: TASK-DUPL-001

#### 3.2.1 任务描述

使用模板方法模式统一指令执行逻辑，消除算术/比较指令的重复代码。

#### 3.2.2 详细步骤

**步骤 1: 创建 AbstractInstructionExecutor 类** (3小时)
```java
// 新类：AbstractInstructionExecutor.java
public abstract class AbstractInstructionExecutor implements InstructionExecutor {
    protected final RegisterOperandExtractor extractor;
    protected final RegisterVMInterpreter vm;
    
    // 模板方法：提取操作数 → 执行计算 → 设置结果
    public void execute(int operand, ExecutionContext context) {
        int rd = extractor.extractRd(operand);
        int rs1 = extractor.extractRs1(operand);
        int rs2 = extractor.extractRs2(operand);
        int result = compute(rs1, rs2, operand);
        vm.setRegister(rd, result);
    }
    
    protected abstract int compute(int rs1, int rs2, int operand);
}
```

**步骤 2: 实现 ArithmeticExecutor** (3小时)
```java
public class ArithmeticExecutor extends AbstractInstructionExecutor {
    private final ArithmeticOperation operation;
    
    public enum ArithmeticOperation {
        ADD, SUB, MUL, DIV
    }
    
    public ArithmeticExecutor(ArithmeticOperation operation) {
        this.operation = operation;
    }
    
    @Override
    protected int compute(int rs1, int rs2, int operand) {
        switch (operation) {
            case ADD: return rs1 + rs2;
            case SUB: return rs1 - rs2;
            case MUL: return rs1 * rs2;
            case DIV: return rs1 / rs2; // 注意：需要检查除零
            default: throw new UnsupportedOperationException("Unknown operation: " + operation);
        }
    }
}
```

**步骤 3: 实现 ComparisonExecutor** (3小时)
```java
public class ComparisonExecutor extends AbstractInstructionExecutor {
    private final ComparisonOperation operation;
    
    public enum ComparisonOperation {
        SLT, SLE, SGT, SGE, SEQ, SNE
    }
    
    @Override
    protected int compute(int rs1, int rs2, int operand) {
        switch (operation) {
            case SLT: return rs1 < rs2 ? 1 : 0;
            case SLE: return rs1 <= rs2 ? 1 : 0;
            case SGT: return rs1 > rs2 ? 1 : 0;
            case SGE: return rs1 >= rs2 ? 1 : 0;
            case SEQ: return rs1 == rs2 ? 1 : 0;
            case SNE: return rs1 != rs2 ? 1 : 0;
            default: throw new UnsupportedOperationException("Unknown operation: " + operation);
        }
    }
}
```

**步骤 4: 迁移现有指令执行器** (3小时)
- ArithmeticExecutors
- ComparisonExecutors
- 验证所有测试通过

#### 3.2.3 验收标准

- [ ] `AbstractInstructionExecutor` 类创建完成
- [ ] 所有算术/比较指令迁移到模板模式
- [ ] 每个具体执行器 < 50 行
- [ ] 所有测试通过
- [ ] 代码重复率进一步降低 ≥ 15%

#### 3.2.4 成功标准

- 指令执行逻辑统一
- 新增指令更容易
- 代码可维护性提升

---

### TASK-DUPL-003: 合并相似指令执行器

**优先级**: 🟡 中 | **预计工时**: 8小时 | **依赖**: TASK-DUPL-002

#### 3.3.1 任务描述

使用参数化执行器合并相似指令，进一步减少代码重复。

#### 3.3.2 详细步骤

**步骤 1: 合并算术指令执行器** (2小时)
- 将 ADD、SUB、MUL、DIV 执行器合并为一个 `ArithmeticExecutor`
- 使用枚举参数区分不同操作

**步骤 2: 合并比较指令执行器** (2小时)
- 将 SLT、SGT、SEQ、SNE 执行器合并为一个 `ComparisonExecutor`
- 使用枚举参数区分不同操作

**步骤 3: 合并逻辑指令执行器** (2小时)
- 将 AND、OR、XOR 执行器合并为一个 `LogicalExecutor`
- 使用枚举参数区分不同操作

**步骤 4: 验证和测试** (2小时)
- 运行所有测试
- 验证性能没有明显下降

#### 3.3.3 验收标准

- [ ] 减少 15-20 个执行器类
- [ ] 代码行数减少 ≥ 30%
- [ ] 所有测试通过
- [ ] 性能无明显下降

#### 3.3.4 成功标准

- 执行器类数量显著减少
- 代码更加简洁
- 易于维护和扩展

---

## 4. 上帝类拆分任务

### TASK-GOD-001: 拆分 CallingConventionUtils（607 行）

**优先级**: 🔴 高 | **预计工时**: 16小时 | **依赖**: 无

#### 4.1.1 任务描述

将 607 行的 `CallingConventionUtils` 上帝类拆分为多个专注类。

#### 4.1.2 详细步骤

**步骤 1: 创建 RegisterSaver 类** (3小时)
```java
// 新类：RegisterSaver.java
public class RegisterSaver {
    /**
     * 保存调用者保存寄存器到栈帧
     */
    public static void saveCallerRegisters(StackFrame frame, int regMask) {
        // 实现保存逻辑
    }
    
    /**
     * 从栈帧恢复调用者保存寄存器
     */
    public static void restoreCallerRegisters(StackFrame frame, int regMask) {
        // 实现恢复逻辑
    }
    
    /**
     * 保存被调用者保存寄存器到栈帧
     */
    public static void saveCalleeRegisters(StackFrame frame, int regMask) {
        // 实现保存逻辑
    }
    
    /**
     * 从栈帧恢复被调用者保存寄存器
     */
    public static void restoreCalleeRegisters(StackFrame frame, int regMask) {
        // 实现恢复逻辑
    }
}
```

**步骤 2: 创建 StackFrameManager 类** (4小时)
```java
// 新类：StackFrameManager.java
public class StackFrameManager {
    /**
     * 计算栈帧大小
     */
    public static int calculateFrameSize(int numCalleeRegs, int numLocals, int numStackArgs) {
        // 实现计算逻辑
    }
    
    /**
     * 计算 SP 调整量
     */
    public static int calculateSpAdjustment(int numCalleeRegs, int numLocals) {
        // 实现计算逻辑
    }
    
    /**
     * 生成函数序言指令序列
     */
    public static String generatePrologue(int numLocals, int usedCalleeRegMask) {
        // 实现生成逻辑
    }
    
    /**
     * 生成函数尾声指令序列
     */
    public static String generateEpilogue(int numLocals, int usedCalleeRegMask) {
        // 实现生成逻辑
    }
}
```

**步骤 3: 创建 ParameterPassing 类** (5小时)
```java
// 新类：ParameterPassing.java
public class ParameterPassing {
    /**
     * 获取参数在栈上的偏移量
     */
    public static int getStackArgOffset(int argIndex) {
        // 实现计算逻辑
    }
    
    /**
     * 获取参数寄存器
     */
    public static int getArgRegister(int argIndex) {
        // 实现计算逻辑
    }
    
    /**
     * 获取参数寄存器的 ABI 名称
     */
    public static String getArgRegisterName(int argIndex) {
        // 实现计算逻辑
    }
    
    /**
     * 生成参数传递指令序列
     */
    public static String generateArgPassing(int argIndex, String value, boolean isImmediate) {
        // 实现生成逻辑
    }
    
    /**
     * 获取返回值寄存器
     */
    public static int getReturnValueRegister() {
        return 2; // a0 (r2)
    }
    
    /**
     * 生成返回值设置指令
     */
    public static String generateReturnValue(String value, boolean isImmediate) {
        // 实现生成逻辑
    }
}
```

**步骤 4: 创建 ABIValidator 类** (2小时)
```java
// 新类：ABIValidator.java
public class ABIValidator {
    /**
     * 验证 ABI 寄存器使用是否正确
     */
    public static boolean validateAbiRegisterUsage(int regNum, String regType) {
        // 实现验证逻辑
    }
    
    /**
     * 验证函数调用是否符合 ABI
     */
    public static String validateCallingConvention(int callerSavedMask, int calleeSavedMask,
                                                  int numArgs, int numLocals) {
        // 实现验证逻辑
    }
    
    /**
     * 生成 ABI 使用报告
     */
    public static String generateABIReport(String functionName, int numArgs, int numLocals,
                                           int callerSavedMask, int calleeSavedMask) {
        // 实现生成逻辑
    }
}
```

**步骤 5: 简化 CallingConventionUtils 为外观类** (2小时)
```java
public class CallingConventionUtils {
    // 仅保留外观方法，委托给上述类
    public static void saveCallerRegisters(StackFrame frame, int regMask) {
        RegisterSaver.saveCallerRegisters(frame, regMask);
    }
    
    public static int calculateFrameSize(int numCalleeRegs, int numLocals, int numStackArgs) {
        return StackFrameManager.calculateFrameSize(numCalleeRegs, numLocals, numStackArgs);
    }
    
    // ... 其他委托方法
}
```

#### 4.1.3 验收标准

- [ ] 所有新类创建完成
- [ ] `CallingConventionUtils` 简化为外观类
- [ ] 每个新类职责单一
- [ ] 所有测试通过
- [ ] 平均类长度 < 200 行

#### 4.1.4 成功标准

- 上帝类拆分完成
- 类职责清晰
- 易于维护和测试

---

### TASK-GOD-002: 重构 RegisterVMInterpreter.executeInstruction()

**优先级**: 🔴 高 | **预计工时**: 12小时 | **依赖**: TASK-GOD-001

#### 4.2.1 任务描述

将 300+ 行的 `executeInstruction()` 方法降低到 < 100 行，使用策略模式和工厂模式。

#### 4.2.2 详细步骤

**步骤 1: 识别特殊指令** (2小时)
- HLT、CALL、RET、J、JT、JF、LF、LS 等需要访问 VM 内部状态的指令

**步骤 2: 创建 SpecialInstructionHandler 类** (3小时)
```java
// 新类：SpecialInstructionHandler.java
public class SpecialInstructionHandler {
    private final RegisterVMInterpreter vm;
    
    public SpecialInstructionHandler(RegisterVMInterpreter vm) {
        this.vm = vm;
    }
    
    public void handleHalt(int operand) {
        vm.setRunning(false);
    }
    
    public void handleJump(int operand) {
        int target = extractImm26(operand);
        if (target < 0 || target >= vm.getCodeSize() || target % 4 != 0) {
            throw new IllegalArgumentException("Invalid jump target: " + target);
        }
        vm.setJumpTarget(target);
    }
    
    public void handleJumpIfTrue(int operand) {
        int rs1 = extractRs1(operand);
        int target = extractImm16(operand);
        if (vm.getRegister(rs1) != 0) {
            vm.setJumpTarget(target);
        }
    }
    
    // ... 其他特殊指令处理
}
```

**步骤 3: 重构 executeInstruction() 方法** (4小时)
```java
private void executeInstruction(int opcode, int operand) throws Exception {
    // 1. 特殊指令检查
    if (isSpecialInstruction(opcode)) {
        handleSpecialInstruction(opcode, operand);
        return;
    }
    
    // 2. 通过工厂获取执行器
    InstructionExecutor executor = instructionFactory.getExecutor(opcode);
    
    // 3. 统一执行
    ExecutionContext context = new ExecutionContext(this, programCounter);
    executor.execute(operand, context);
}
```

**步骤 4: 创建 InstructionFactory 类** (2小时)
```java
// 新类：InstructionFactory.java
public class InstructionFactory {
    private final Map<Integer, InstructionExecutor> executorMap;
    
    public InstructionFactory() {
        executorMap = new HashMap<>();
        registerExecutors();
    }
    
    private void registerExecutors() {
        executorMap.put(ADD_OPCODE, new ArithmeticExecutor(ArithmeticOperation.ADD));
        executorMap.put(SUB_OPCODE, new ArithmeticExecutor(ArithmeticOperation.SUB));
        // ... 注册所有指令
    }
    
    public InstructionExecutor getExecutor(int opcode) {
        return executorMap.getOrDefault(opcode, 
            (op, ctx) -> { throw new UnsupportedOperationException("Unsupported opcode: " + opcode); });
    }
}
```

**步骤 5: 验证和测试** (1小时)

#### 4.2.3 验收标准

- [ ] `executeInstruction()` 方法 < 100 行
- [ ] 圈复杂度 ≤ 10
- [ ] 所有测试通过
- [ ] 代码可读性提升

#### 4.2.4 成功标准

- 方法复杂度降低
- 代码结构清晰
- 易于维护和扩展

---

### TASK-GOD-003: 引入指令工厂

**优先级**: 🟡 中 | **预计工时**: 8小时 | **依赖**: TASK-GOD-002

#### 4.3.1 任务描述

创建 `InstructionFactory` 类，统一管理指令执行器。

#### 4.3.2 详细步骤

**步骤 1: 定义 InstructionFactory 类** (2小时)
```java
public class InstructionFactory {
    private final Map<Integer, InstructionExecutor> executorMap;
    private final Map<Integer, String> opcodeToName;
    
    public InstructionFactory() {
        executorMap = new HashMap<>();
        opcodeToName = new HashMap<>();
        registerExecutors();
    }
    
    private void registerExecutors() {
        // 注册所有 42 条指令
    }
    
    public InstructionExecutor getExecutor(int opcode) {
        return executorMap.getOrDefault(opcode, 
            (op, ctx) -> { throw new UnsupportedOperationException("Unsupported opcode: " + opcode); });
    }
    
    public String getInstructionName(int opcode) {
        return opcodeToName.getOrDefault(opcode, "UNKNOWN");
    }
}
```

**步骤 2: 注册所有指令** (4小时)
- 算术指令（ADD、SUB、MUL、DIV）
- 比较指令（SLT、SGT、SEQ、SNE）
- 逻辑指令（AND、OR、XOR）
- 浮点指令（FADD、FSUB、FMUL、FDIV）
- 控制流指令（CALL、RET、J、JT、JF）
- 加载/存储指令（LI、LW、SW、LF、LS）
- 等等...

**步骤 3: 集成到 RegisterVMInterpreter** (1小时)

**步骤 4: 测试验证** (1小时)

#### 4.3.3 验收标准

- [ ] `InstructionFactory` 类创建完成
- [ ] 所有 42 条指令注册到工厂
- [ ] 所有测试通过
- [ ] 支持动态添加新指令

#### 4.3.4 成功标准

- 指令管理统一
- 易于扩展新指令
- 代码组织清晰

---

## 5. 接口抽象优化任务

### TASK-ABSTRACT-001: 定义核心接口

**优先级**: 🟡 中 | **预计工时**: 10小时 | **依赖**: 无

#### 5.1.1 任务描述

为核心组件定义接口，支持依赖注入。

#### 5.1.2 详细步骤

**步骤 1: 定义 IVirtualMachine 接口** (2小时)
```java
// 新接口：IVirtualMachine.java
public interface IVirtualMachine {
    void load(InputStream input) throws Exception;
    void execute() throws Exception;
    int getRegister(int regNum);
    void setRegister(int regNum, int value);
    VMConfig getConfig();
    void setTrace(boolean trace);
}
```

**步骤 2: 定义 IInstructionExecutor 接口** (2小时)
```java
// 新接口：IInstructionExecutor.java
public interface IInstructionExecutor {
    void execute(int operand, ExecutionContext context) throws Exception;
    default boolean isSpecialInstruction() { return false; }
}
```

**步骤 3: 定义 IVMConfig 接口** (2小时)
```java
// 新接口：IVMConfig.java
public interface IVMConfig {
    int getHeapSize();
    int getLocalsSize();
    int getMaxCallStackDepth();
    int getMaxExecutionSteps();
}
```

**步骤 4: 更新现有类实现接口** (3小时)
- RegisterVMInterpreter 实现 IVirtualMachine
- 所有执行器类实现 IInstructionExecutor
- VMConfig 实现 IVMConfig

**步骤 5: 添加 JavaDoc** (1小时)

#### 5.1.3 验收标准

- [ ] 所有核心接口定义完成
- [ ] 接口包含完整的 JavaDoc
- [ ] 现有类实现对应接口
- [ ] 所有测试通过

#### 5.1.4 成功标准

- 接口抽象清晰
- 依赖注入支持
- 易于测试和mock

---

### TASK-ABSTRACT-002: 重构包结构

**优先级**: 🟡 中 | **预计工时**: 12小时 | **依赖**: TASK-ABSTRACT-001

#### 5.2.1 任务描述

按职责重新组织 `stackvm` 包，提高内聚性。

#### 5.2.2 详细步骤

**步骤 1: 创建新的包结构** (1小时)
```
org.teachfx.antlr4.ep18r.stackvm/
├── interpreter/           # 解释器核心
│   ├── RegisterVMInterpreter.java
│   ├── IVirtualMachine.java
│   └── ExecutionContext.java
├── instructions/          # 指令系统
│   ├── factory/          # InstructionFactory
│   ├── executors/        # 各种执行器
│   ├── strategies/       # 执行策略
│   └── model/            # 指令数据模型
├── memory/               # 内存管理
│   ├── RegisterFile.java
│   ├── MemorySpace.java
│   └── HeapManager.java
├── registers/            # 寄存器系统
│   ├── RegisterFile.java
│   ├── RegisterAllocator.java
│   └── RegisterOperandExtractor.java
├── callingconvention/    # 调用约定
│   ├── RegisterSaver.java
│   ├── StackFrameManager.java
│   ├── ParameterPassing.java
│   └── ABIValidator.java
├── config/               # 配置系统
│   ├── VMConfig.java
│   ├── VMConfigBuilder.java
│   └── IVMConfig.java
└── exception/            # 异常体系
    ├── VMException.java
    ├── InvalidOpcodeException.java
    └── DivisionByZeroException.java
```

**步骤 2: 迁移现有类到新包** (8小时)
- 解释器类 → interpreter/
- 指令相关类 → instructions/
- 内存相关类 → memory/
- 寄存器相关类 → registers/
- 调用约定类 → callingconvention/
- 配置类 → config/
- 异常类 → exception/

**步骤 3: 更新包导入** (2小时)

**步骤 4: 验证和测试** (1小时)

#### 5.2.3 验收标准

- [ ] 新包结构创建完成
- [ ] 所有类迁移到新位置
- [ ] 包导入更新完成
- [ ] 所有测试通过
- [ ] 包内聚性提升

#### 5.2.4 成功标准

- 包结构清晰
- 职责分离明确
- 易于导航和维护

---

### TASK-ABSTRACT-003: 实现依赖注入

**优先级**: 🟡 中 | **预计工时**: 8小时 | **依赖**: TASK-ABSTRACT-002

#### 5.3.1 任务描述

使用构造器注入替代硬编码依赖，提高可测试性。

#### 5.3.2 详细步骤

**步骤 1: 重构 RegisterVMInterpreter 构造器** (3小时)
```java
public class RegisterVMInterpreter implements IVirtualMachine {
    private final IVMConfig config;
    private final IInstructionFactory instructionFactory;
    private final IRegisterFile registerFile;
    private final IMemoryManager memoryManager;
    
    public RegisterVMInterpreter(IVMConfig config, 
                                 IInstructionFactory factory,
                                 IRegisterFile registers,
                                 IMemoryManager memory) {
        this.config = config;
        this.instructionFactory = factory;
        this.registerFile = registers;
        this.memoryManager = memory;
    }
    
    // 保留默认构造器（使用默认实现）
    public RegisterVMInterpreter() {
        this(new VMConfig.Builder().build(),
             new InstructionFactory(),
             new RegisterFile(),
             new MemoryManager());
    }
}
```

**步骤 2: 重构其他核心类** (3小时)
- InstructionFactory
- RegisterFile
- MemoryManager

**步骤 3: 添加测试支持** (1小时)

**步骤 4: 验证和测试** (1小时)

#### 5.3.3 验收标准

- [ ] 核心类支持依赖注入
- [ ] 依赖通过构造器传入
- [ ] 所有测试通过
- [ ] 可测试性提升

#### 5.3.4 成功标准

- 依赖注入实现
- 易于单元测试
- 解耦提升

---

## 6. 测试增强任务

### TASK-TEST-001: 测试策略升级

**优先级**: 🟡 中 | **预计工时**: 16小时 | **依赖**: TASK-BINFRA-002

#### 6.1.1 任务描述

完善测试策略，提升测试覆盖率至 ≥ 85%。

#### 6.1.2 详细步骤

**步骤 1: 单元测试增强** (6小时)
- 为每个核心类编写单元测试
- 目标覆盖率 ≥ 90%
- 重点测试：
  - RegisterVMInterpreter
  - InstructionFactory
  - 所有执行器类

**步骤 2: 集成测试添加** (6小时)
- 测试组件间交互
- 重点测试：
  - 完整编译-执行流程
  - 函数调用栈帧切换
  - 调用约定兼容性
  - 异常处理流程

**步骤 3: 性能测试建立** (4小时)
- JMH 基准测试框架
- 对比栈式 vs 寄存器式虚拟机
- 测试指标：
  - 指令执行速度
  - 内存使用
  - 函数调用开销

#### 6.1.3 验收标准

- [ ] 测试覆盖率 ≥ 85%
- [ ] 核心类覆盖率 ≥ 90%
- [ ] 集成测试用例 ≥ 20 个
- [ ] 性能基准测试建立

#### 6.1.4 成功标准

- 测试覆盖全面
- 测试质量提升
- 性能基准确立

---

### TASK-TEST-002: 新增测试类

**优先级**: 🟡 中 | **预计工时**: 12小时 | **依赖**: TASK-TEST-001

#### 6.2.1 任务描述

创建新的测试类，补充测试覆盖。

#### 6.2.2 详细步骤

**步骤 1: RegisterVMInterpreterUnitTest** (4小时)
- 200+ 测试方法
- 覆盖所有寄存器操作
- 覆盖所有指令执行
- 覆盖异常处理

**步骤 2: InstructionExecutorTest** (3小时)
- 测试所有执行器类
- 参数化测试
- 边界条件测试

**步骤 3: CallingConventionIntegrationTest** (3小时)
- 测试调用约定兼容性
- 测试栈帧布局
- 测试参数传递
- 测试返回值处理

**步骤 4: RegisterAllocationTest** (2小时)
- 测试寄存器分配
- 测试寄存器溢出
- 测试寄存器释放

#### 6.2.3 验收标准

- [ ] 所有新测试类创建完成
- [ ] 测试命名遵循规范
- [ ] 测试覆盖率达标
- [ ] 所有测试通过

#### 6.2.4 成功标准

- 测试用例全面
- 测试质量高
- 易于维护

---

## 7. 文档完善任务

### TASK-DOC-001: 架构文档

**优先级**: 🟢 低 | **预计工时**: 4小时 | **依赖**: 所有架构优化任务

#### 7.1.1 任务描述

同步最新实现状态，更新接口说明。

#### 7.1.2 详细步骤

**步骤 1: 更新架构设计规范** (2小时)
- 同步最新的包结构
- 更新接口定义
- 添加新的设计模式说明

**步骤 2: 更新类图和序列图** (2小时)
- 更新类图反映新架构
- 更新序列图反映新的调用流程

#### 7.1.3 验收标准

- [ ] 架构文档与实现同步
- [ ] 所有接口有详细说明
- [ ] 类图和序列图准确

#### 7.1.4 成功标准

- 文档完整准确
- 易于理解
- 有助于学习

---

### TASK-DOC-002: 代码规范

**优先级**: 🟢 低 | **预计工时**: 6小时 | **依赖**: 所有代码优化任务

#### 7.2.1 任务描述

为所有公共类/方法添加完整 Javadoc。

#### 7.2.2 详细步骤

**步骤 1: 添加类级别 Javadoc** (2小时)
- 所有 public 类
- 所有接口
- 所有枚举

**步骤 2: 添加方法级别 Javadoc** (3小时)
- 所有 public 方法
- 所有 protected 方法
- 关键 private 方法

**步骤 3: 添加参数和返回值说明** (1小时)

#### 7.2.3 验收标准

- [ ] Javadoc 覆盖率 ≥ 80%
- [ ] 所有公共 API 有文档
- [ ] 文档清晰准确

#### 7.2.4 成功标准

- 代码文档完整
- 易于理解和使用
- 符合项目标准

---

## 8. 附录

### 8.1 任务优先级说明

- **🔴 高优先级**: 必须在 2 周内完成
- **🟡 中优先级**: 应在 4 周内完成
- **🟢 低优先级**: 可选，有时间再完成

### 8.2 任务依赖关系

- 基础设施恢复任务：无依赖，应优先完成
- 代码重复消除任务：基础设施恢复完成后开始
- 上帝类拆分任务：代码重复消除完成后开始
- 接口抽象优化任务：上帝类拆分完成后开始
- 测试增强任务：所有代码优化任务完成后开始
- 文档完善任务：所有任务完成后开始

### 8.3 工时估算说明

- 工时为估算值，实际可能 ±20%
- 包含测试和文档时间
- 不包含审查和修改时间

---

**最后更新**: 2026-01-08
**维护者**: Claude Code
**状态**: ✅ 任务分解完成，准备执行
