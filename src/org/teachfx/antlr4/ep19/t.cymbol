struct Point {
    int x = 0;
    int y = 0;

    // init(int ix,int iy) {
    //   this.x = ix;
    //   this.y = iy;
    //
    // void incX() {
    //   this.x = this.x + 1;
    // }

}

int fib(int n) {
    if (n <= 2) return n;
    else return fib(n-1) + fib(n-2);
}

int main() {

    Point s = new Point(1,2);
    int y = 12;
    s.x = 10 + fib(6);
    s.y = 2  + y;
    print(s.x);
    return 0;
}